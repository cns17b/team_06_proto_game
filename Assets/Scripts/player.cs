using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//All things to program for the player character
public class Player : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
	
	//Respond to being hit
	void Hit()
	{
		//Lose hat on first hit
		//Move backwards
		//Stretch vertically
	}
	//Check if there is ammo, if so, fire projectile
	void Shoot()
	{
		if ammo > 0
			//Shoot
		else
			//Display out of ammo message
	}
}